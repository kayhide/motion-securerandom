require 'spec_helper'

describe Motion::SecureRandom do
  it 'has a version number' do
    expect(Motion::SecureRandom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
